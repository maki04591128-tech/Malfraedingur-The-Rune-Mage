# INC-2 検証仕様 — 戦闘の手応えと「気持ちよさ判定」

| 項目 | 内容 |
|------|------|
| 役割 | INC-1 で確認したコア面白さを **戦闘層に持ち込んだ瞬間** に劣化しないか、を判定するための検証ツールと合否基準 |
| 対象 | INC-2（旧 M2）。`02` §3 「戦闘の手触り」 |
| ステータス | Draft v0.1（骨格のみ。実装ピンポンで v0.2 以降で厚くする） |

## 更新履歴
- v0.1 初版（骨格のみ）。§0 仮説／§1 機能要件／§2 スコープ／§3 必須シナリオ S1〜S5 を固定。§4 通過/kill 基準は v0.3 で確定する。
- v0.2 v0.1 実装 (combat_test.tscn) を実機で動かして見えた問題を書き戻し。バランス補正 (DAMAGE_SCALE=3.0)、暴発演出の不足、ボス出現演出、HP バー視認性、sjalfr 経路の特例（スケール対象外）を §1.3/§1.4 に追加。combat シナリオ JSON C1〜C5 を `tests/scenarios/combat/` に追加。
- v0.3 §4 通過条件と kill 基準を確定。観測可能な指標（時間・人数・体感の言語化）で判定可能化。
- v0.4 combat_test に **A+B+C+D** を実装: A=シナリオ C1〜C5 ドロップダウン（index.json から自動ロード、選択で ruleset/C/敵パラ/tokens を一括適用）、B=ruleset 切替＋C スライダ、C=チューニングパネル（DAMAGE_SCALE / 雑魚 HP・ATK / ボス HP・ATK）、D=ボス出現演出（雑魚撃破直後に「⚔ ボス出現」表示＋ボス名を赤系色付け）＋暴発演出（カテゴリ別の色とアイコン）。`DAMAGE_SCALE` を `const` → `static var` 化（ライブ調整可能化）。**E**=test_smoke に SpellEngine→CombatSystem 統合 E2E テストを追加（連続詠唱で雑魚撃破ターン数・C=100 暴発ゼロ・DAMAGE_SCALE ライブ書き換え反映を確認）。
- **v0.5 マス目移動・グリッド戦術導入の大改訂を反映**（2026-05-23 ユーザー判断、`01 v0.9` / `02 v0.9` / `03 v0.18` / `04 v0.6` / 新規 `09` と一体）。本仕様の C1〜C5 シナリオは **位置なし combat_test で実施した既存検証** を正典として維持（INC-2 v0.4 完了時点で 272 アサーション ALL GREEN・H2 真）。v0.5 では**新規 §6「INC-3.5 における新仕様での H2 再判定」**を末尾追加。シームレス戦闘 + 範囲語・方向語コア化の状態で本 §3 シナリオを再走したとき、H2 が依然真であるかを確認する手順と通過条件を規定
- **v0.6 INC-3.5 v0.9.5 実装完了に伴う §6 確定**（2026-05-24）。C6-C8 を test_smoke で自動化（passed: 423/423 ALL GREEN）。C7 の AoE は現状やや強め所感、INC-4 で再 calibration。H2 真維持と判定

---

## 0. なぜこの文書が要るか

`02` §3 INC-2 の学び目標は **「コアの気持ちよさを第三者が触って言語化できるか」**。INC-1 では `spell_lab`/`spell_builder` で「文法を直すと手応えが良くなる」までは確認できたが、それは **戦闘の文脈なし** での話。戦闘 UI・ターン・敵 HP・世界時間 Δ が乗った瞬間に、INC-1 の手応えが消える/見えづらくなる可能性がある。

検証する仮説（H2）:

> **H2: 「呪文を組んで敵に当て、誤射の原因が分かり、次の詠唱で改善できる」体験は、INC-1 の手応えを戦闘層でも失わずに増幅する。**

具体的サブ仮説:
- H2a: 詠唱→ダメージ→敵 HP 減のフィードバックが、`GrammarReport` 単独より「やった」感を強める。
- H2b: 誤射時、戦闘 UI 上でも「なぜ外れた／弱かった」が即わかる。原因が「敵が悪い」ではなく「自分の文法が悪い」と帰責される。
- H2c: 雑魚 1 体 → ボス 1 体の最小プログレッションが「もう一回」を生む。

H2 が偽（戦闘層で手応えが消える）なら、`02` §4 の方向転換：戦闘 UI の見せ方を再設計するか、INC-1 の枠組み自体を再考。**INC-3（7日ループ）には進まない。**

---

## 1. 機能要件（combat_test の UI/挙動）

`scenes/combat/combat_test.tscn`（新設）。`spell_builder` の上に戦闘層を被せる構造。

### 1.1 戦闘エンティティ
- **Player**: HP（既定 100）、習得語の一覧、scaffold_level 設定。
- **Enemy（雑魚）**: HP（既定 30〜50）、属性耐性（任意）、`AttackPattern`（ターン経過で固定ダメージを Player に返す簡易 AI で十分）。
- **Boss**: HP（既定 120）、属性耐性あり、複数攻撃パターン（INC-2 はパターン 2 種で十分）。

### 1.2 ターン構造（`01` 3.8 準拠）
- プレイヤーターン: 「呪文を 1 つ構築して詠唱」or「待機/学習」を選ぶ → 解決 → 敵ターン。
- 1 詠唱 = 1 プレイヤーターン + 世界時間 Δ（語数 × 基本コスト + ティア加算、`05` `cast_cost`/`world_time_costs` に従う。INC-2 では Δ を表示するだけで使用効果は無くてよい — INC-3 への接続点として可視化する）。
- 敵ターン: 固定 AI で Player.hp を削る。

### 1.3 詠唱解決経路
- 既存 `SpellEngine.cast()` の `CastResult` を再利用。
- 新規 `DamageCalculator`: `effect_power` と element/chaos と Enemy.resist から最終ダメージを計算（最小: `damage = effect_power × elem_mult × DAMAGE_SCALE`、`elem_mult` は雑魚 1.0 固定でも可）。
- **v0.2 補正: `DAMAGE_SCALE = 3.0`**。実機検証で P_base ~ 2.3 vs 敵 HP 30 / Player HP 100 が釣り合わず、雑魚撃破 13+ ターンに対し Player は雑魚反撃 8 ダメ × 12 ターンで死亡することが判明。全戦闘ダメージを底上げして「文法 OK で 3〜5 ターンで雑魚撃破」できる強度に揃える。INC-3 で `BalanceConfig.damage_scale` に外出し予定（現状は `core/combat/damage_calculator.gd` const）。
- **v0.2 特例: sjalfr (自己対象) 経路は DAMAGE_SCALE を掛けない**。誤詠唱で対象タイルを取り違えたとき即死級ダメを受けるのは「文法を試す心理的コスト」を上げすぎる。タイル選択ミスは比較的明確な誤りなので、ペナルティはマイルドに保つ。
- 暴発時の挙動（`03` §5.3 重篤度バンド）を **戦闘層に反映**：
  - 不発 (≤30): ダメージ 0、ターンは消費。
  - 実行失敗 (31–60): ダメージ × 0.5、対象外（隣にいる味方/自分なし時は宙に消える）。
  - 自爆 (61–99): Player.hp に self_damage_ratio × effect_power を加算（DAMAGE_SCALE 適用）。
  - 100 (達人): 暴発しない（INC-1 と整合）。

### 1.4 UI 表示
- HP バー（Player / 現在敵）。
- ターン表示（「あなたのターン / 敵のターン / ターン N」）。
- 詠唱ログ（直近 N 件: 入力呪文 / `GrammarReport` 要約 / ダメージ / 暴発カテゴリ）。
- 世界時間 Δ の累積表示。
- scaffold_level による補助 UI（INC-1 の仕様をそのまま戦闘層に持ち込む）:
  - **max**: ライブプレビュー + ダメージ予測表示。
  - **mid**: ヒント + 詠唱前確認ダイアログ。
  - **low**: 既存（詠唱後にレポート）。
  - **none**: GrammarReport を簡略化（合否のみ）。

### 1.5 進行
- 雑魚 1 体撃破 → Boss 出現 → Boss 撃破 → 「フロアクリア」表示 + 経過ターン数 + 累積 Δ + 詠唱回数を表示。
- Player.hp 0 → 「敗北」表示 + 同統計（INC-3 で巻き戻しに置き換える前提の暫定出口）。
- **v0.2 残課題: ボス出現演出が不足**。雑魚撃破時に「ボス Helgrind 番人が現れた！」のような戦闘ログ強調＋HP バー色変化＋一時停止のいずれかが必要。現状は HP バーが切り替わるだけで「次の段階に進んだ」感が薄い。
- **v0.2 残課題: 暴発演出が不足**。暴発カテゴリ別に色分けやアイコンが欲しい。`activation`=灰／`execution`=黄／`control`=赤 など。実機検証では C=50 base=5% でなかなか暴発しないので、まず scaffold_low の C=0 で意図的に踏むテストが必要。

### 1.6 リセット
- 「最初から」ボタンでフロア初期化（INC-3 の巻き戻し未実装の代替）。

### 1.8 v0.4 で追加された周辺機能（spell_builder と同水準）

- **ruleset OptionButton**: phase_intro / phase_beginner / phase_intermediate / phase_advanced を即切替。scaffold_level の差分を combat 文脈で観察できる（§4.1.1 と §4.1.5 の前提）。
- **C (理解度) HSlider**: 0–100 を実データ蓄積を待たず任意設定。INC-1 の spell_lab と同思想。
- **シナリオ OptionButton**: `tests/scenarios/combat/index.json` から C1〜C5 を自動ロード。選択で ruleset_id / scaffold_level / c_override / enemies.hp/atk / tokens を一括適用してフロアリセット。これにより §3 必須シナリオが「ワンクリックで再現」できる（08 §3 と整合）。
- **チューニングパネル (CheckButton 開閉)**: DAMAGE_SCALE / 雑魚HP / 雑魚ATK / ボスHP / ボスATK のスライダ。`DAMAGE_SCALE` だけは `DamageCalculator.DAMAGE_SCALE = v` で即時反映。敵パラメータは「最初から」で反映（フロア生成時に読み取るため）。
- **ボス出現演出**: `_on_enemy_defeated` ハンドラで雑魚 (index=0) 撃破時に「⚔ ボス出現: <名前>」を戦闘ログへ目立つ赤で出力。ボスの HP/ATK もログに併記。敵名ラベルもボス色（濃赤）に切替。
- **暴発演出**: カテゴリ別の色＋アイコン: `activation`=灰○ / `execution`=橙◇ / `control`=赤✸。暴発時のみログに目立つ行が追加される。意図的に C=0 + scaffold=none で踏むと確認可。

### 1.7 v0.2 で確認済みの動作（実機検証 2026-05-23）
combat_test.tscn を Godot 4.6.2 で起動し、phase_intro (scaffold=mid, C=50 デフォルト) で以下を確認:

1. **正詠唱 (`meiða fjanda`, acc)**: 文法 OK / G=1.00 / Δ=3.0 / 命中 → 敵 2.3 ダメ。HP バー連動 OK。雑魚反撃 8 ダメ。ターン進行 OK。
2. **誤詠唱 (`meiða fjandi`, nom)**: 文法 NG / G=0.50 / 命中 → 敵 1.2 ダメ（約半減）。戦闘ログに「✗ fjandi は対格ではありません → fjandi → fjanda ?」が表示される（INC-1 の GrammarReport が戦闘層でもそのまま機能する）。
3. **暴発判定**: C=50 base=5%, case×2 = 10% 確率で暴発しなかった（試行 2 回）。意図的に踏むには C=0 + scaffold=none が必要。v0.2 では確認まで至らず。

→ **H2a（フィードバックの増幅）は v0.2 時点で予備的に肯定**: GrammarReport の即時性 + HP バー減少 + ターン進行の組み合わせは、INC-1 の Report 単独より「やった/外した」が分かりやすい。ただし統計は試行 2 回のみ。C1〜C5 シナリオで正式判定する。

---

## 2. スコープ

### 2.1 含める
- 1 フロア = 雑魚 1 体 + ボス 1 体（02 §3 INC-2 通り）。
- コマンド入力型ターン制。
- 入門 + 初級ルール（`03` §6）の範囲: 効果語 + 対象語（4 格） + 四大元素 + 修飾語 mikill/litill。
- scaffold_level 4 段階の戦闘内切替（INC-1 と同じ実装をそのまま使う）。

### 2.2 含めない（INC-3 以降）
- 7 日ループ、巻き戻し、`öld renna aptr`。
- フロア生成（複数フロア）。
- 移動/探索（コマンドに「待機」だけ用意して接続点を作る）。
- 語彙発見、理解度上昇の 3 経路（INC-4）。
- セーブ/ロード。

### 2.3 統計信頼性
INC-1 と異なり、戦闘は **連続詠唱** で評価する。S1〜S5 を各 5 回ずつ通しでプレイして、(a) 雑魚撃破までの平均ターン数 (b) ボス撃破率 (c) 暴発回数の中央値 を記録する。N=200 のバッチは戦闘層には不要（Resolver 層の確率検証は INC-1 で済）。

---

## 3. 必須シナリオ（毎セッション走らせる固定セット）

`tests/scenarios/combat/` 配下に JSON で固定化。combat_test の「シナリオ」ドロップダウンから一括呼び出し可能にする。

| ID | 状況 | scaffold | C | 期待される観測（質的） |
|----|------|----------|---|------------------------|
| C1 | 正しい呪文 `meiða fjanda` で雑魚を撃破 | low | 50 | 2〜3 ターンで雑魚撃破。ダメージ表記と HP バーが連動。「効いている」感が出る |
| C2 | 誤格 `meiða fjandi` を撃ってしまう | low | 50 | ダメージ激減 or 暴発。**戦闘ログで `fjandi → fjanda` が提示され、次ターンで自力修正できる** |
| C3 | 雑魚 → ボスの遷移 | mid | 50 | 雑魚撃破時に Boss が出現するのが視覚的に分かる。Boss は雑魚より明らかにタフで、修飾語 `mikill` の重要性が体感される |
| C4 | scaffold_max で初学者プレイ想定 | max | 0 | ライブプレビュー + ダメージ予測のおかげで、ノー知識でも 5 分以内に雑魚を倒せる |
| C5 | scaffold_none で達人プレイ想定 | none | 100 | 補助なしでもボスを安定撃破。暴発ゼロ、誤射ゼロ |

シナリオは `seed` 固定で再現性を確保（`03` §8 と整合）。

---

## 4. 気持ちよさの合否基準（v0.3 確定）

### 4.1 通過条件（すべて満たす）

1. **発見可能性（戦闘版・C4 シナリオ）**: phase_intro (scaffold=max) + C=0 で説明なしに combat_test を渡し、雑魚 fjandi を **5 分以内に自力で撃破** できる。ライブプレビューの「✓ プレビュー: 文法 OK」を頼りに `meiða fjanda` を組めるか。
2. **手応えの可視性（C1 シナリオ）**: 正詠唱でダメージ + HP バー減少が、INC-1 の `GrammarReport` 単独より「やった」感を上回ると **開発者本人が言語化** できる。具体的には combat_test と spell_builder で同じ呪文を撃ち比べて、「戦闘の方が確かに気持ちいい」と即答できる。
3. **誤射→修正→命中のループ（C2 シナリオ）**: 文法 NG 詠唱後、戦闘ログの推奨修正を見て **次ターン以内に自力で正詠唱できる**。被験者が「fjandi → fjanda」の対応を口に出す。
4. **再挑戦意欲**: 敗北画面で **5 名中 3 名以上が「最初から」ボタンを自発的に押す**（一回で諦めない）。
5. **第三者言語化**: 開発者以外の被験者 **2 名以上が「面白い」「悔しい」「次はこうする」のいずれかを口に出す**（声に出す観察、`02` §0「学んだことを言語化」の戦闘層版）。

### 4.2 計測（記録項目）

- C1 で雑魚撃破までの平均ターン数（目標: 3〜6 ターン、`tests/scenarios/combat/c1_*.json` の expected_outcome）。
- C4 で「最初の正しい呪文」までの時間（目標 < 5 分、`01` 8 章・INC-1 §4 と整合）。
- C5 で暴発回数 (目標 0、達人不変条件 T6 が戦闘層でも保たれる確認)。
- 各観点を 5 段階自己申告（1=退屈 5=やめられない）。中央値で見る。

### 4.3 kill 基準（H2 が偽の判定）

次のいずれかが、**v0.2 の DAMAGE_SCALE 含むチューニングを尽くしても**該当する場合、H2 は偽:

- **§4.1.1 (発見可能性) が満たせない**: scaffold=max でも 5 分以内に雑魚撃破できない被験者が多数（コアが自明でない）。
- **§4.1.2 (手応えの可視性) が満たせない**: 「戦闘 UI を被せても spell_builder と感じが変わらない」または「戦闘 UI のせいで GrammarReport が見えづらくなった」と感じる。
- **誤射の帰責が「ゲームのせい」になる**: 暴発時に被験者が「ゲームが理不尽」と感じる（「自分の文法が悪かった」ではなく）。
- **第三者が要約で言語学要素を落とす**: 被験者が「タイル選んでボタン押すだけ」と要約する（古ノルド語の格・語順を意識しない）。

→ `02` §4 の方向転換: (a) 戦闘 UI のフィードバックを再設計、(b) DAMAGE_SCALE/HP のバランスを大幅変更、(c) それでも偽なら INC-1 の枠組み自体（C・G・暴発の関係）に立ち戻る。**INC-3（7 日ループ）には進まない**。

---

## 5. 進行判断（このゲートの出口）

| 結果 | 次アクション |
|------|--------------|
| 自動テスト緑 + §4 通過条件（v0.3 確定後）全達成 | INC-3（7 日ループ）へ進む |
| 自動テスト緑 + §4 一部不通過 | UI/フィードバック調整。原因を C1〜C5 のどこで割れるか特定 |
| §4 kill 基準該当 | 戦闘 UI 再設計、または INC-1 の枠組みに立ち戻る |
| 自動テスト赤 | バグ修正（気持ちよさ判定はその後） |

---

## 6. INC-3.5 における新仕様での H2 再判定（v0.5 / v0.9.5 完了）

INC-3.5 で位置あり戦闘 + 範囲語・方向語コア化を導入した状態で、本仕様 §3 の C1〜C5 シナリオと H2 が依然成立するかを再評価する章。シームレス戦闘で「位置を取って撃つ／撃たれる」体感が加わったとき、H2 の核（正しい詠唱は気持ちよく、間違うとペナルティが手応えとして感じられる）が壊れていないか確認する。

### 6.1 再判定の前提

- ruleset: `phase_intermediate.tres`
- 位置: 30×25 マップ（INC-3.5 で 40×30 から縮小）
- SpatialContext を engine.cast の options に渡し、座標 + 視界 + 敵位置を解決
- 暴発倍率: `range_required=2.0` / `range_conflict=3.0` / `direction_required=2.0`
- シナリオは `tests/test_smoke.gd` の「INC-3.5 v0.9.5: ... C6-C8」セクションで自動化

### 6.2 C6-C8 新規シナリオ

| ID | 詠唱 | 配置 | 期待 | 自動テスト |
|----|------|------|------|-----------|
| C6 | `fjarri fram meiða fjanda` | プレイヤー (10, 10) 北、敵 front_close (10, 9) 距離 1 | `fjarri` は射程 3-8 のため隣接敵は射程外 → `range_required` finding、`overall_pass=false`、SpatialResolver マージ経由でコア違反扱い | `C6: ...` 群 |
| C7 | `vítt fram meiða fjanda` | 中心 = プレイヤー北 ×2 = (10, 8)、半径 2 円内に敵 3 体配置 | `target_tiles.size() >= 3`（AoE で複数体撃破の気持ちよさ） | `C7: ...` |
| C8 | `í gegnum fram meiða fjanda` | 直線上に壁 (10, 7) と壁の向こうの敵 (10, 5) | `ignores_walls=true` で貫通、wall_beyond を 1 体当てる | `C8: ...` 群 |

### 6.3 通過条件（H2 が新仕様でも真）

- C6-C8 全パス（v0.9.5 達成 ✓）
- C1-C5 の既存シナリオ（位置なし combat_test）が phase_intermediate でも全パス維持（v0.9.5 達成 ✓）
- AoE / 貫通の「複数体に当たる気持ちよさ」が test_smoke 上で証明可能（`target_tiles.size()` で検証）
- 「間違うとペナルティ」がコア違反 finding の暴発倍率（range/direction 系 ×2-3）で実装

### 6.4 kill 基準（H2 が偽の判定）

- C7 で AoE の damage_split = 1.0（敵 N 体に effect_power × N の総ダメ）が「強すぎる／弱すぎる」体感 → `09 §11` のオープン項目 calibration、`05 BalanceConfig.aoe.damage_split` を導入してチューニング可能化
- C8 で貫通の壁無視がコア体験「壁陰に隠れて学ぶ」を壊す → `í gegnum` を後ろ倒し、または無辞書経路限定に
- C6 の range_required 暴発（×2）が「直したくなる」強度として弱い／強すぎる → 倍率調整（`MISFIRE_MULT_BY_RULE.range_required`）

→ **v0.9.5 では kill 基準に該当せず**。C7 の AoE バランスは「3 体に当てて 30 ダメ × 3 = 90 ダメ」で雑魚 1 体分の HP（30）を大きく超え、現状はやや強い体感（INC-4 でアーティファクト追加と一緒に再 calibration 予定）。H2 は新仕様でも維持されると判定。

> 本ゲートは「戦闘層に乗せた瞬間にコアが消えないか」を見る装置。kill 判定はプロジェクトの失敗ではなく、設計された前進（`02` §0/§4）。

---

参照: 全体目標 → `01` / 計画 → `02`（INC-2） / 数式・不変条件 → `03` §5/§6 / 実装構造 → `04` / INC-1 検証仕様 → `06`（テンプレ元） / マップ・移動 → `09`

---

## 6. INC-3.5 における新仕様での H2 再判定（v0.5 追加）

`02 v0.9` で新設された **INC-3.5** は、シームレス戦闘 + 範囲語・方向語コア化の状態で本 §3 シナリオ C1〜C5 と `06` S1〜S6 を再走し、H/H2 が新仕様でも成立するか確認する。本章は INC-3.5 実装時に走らせる H2 再判定手順を規定する。

### 6.1 なぜ再判定が要るか

INC-2 v0.4 完了時点で H2 は予備的に真（272 アサーション緑、ユーザー実機で C3「雑魚 → ボス遷移」とボス出現演出を確認）。だがそれは **位置なし combat_test** での話。`01 v0.9` でシームレス戦闘になり、`03 v0.18` で範囲語・方向語がコアに昇格した状態で、戦闘層の手応えが維持されるかを確認する必要がある。

具体的な懸念:

- **H2a（フィードバック増幅）**: 位置あり詠唱が増えた結果、GrammarReport の情報量が増えすぎて「文法を直すと手応えが出る」のシグナル/ノイズ比が悪化していないか
- **H2b（誤射の自己帰責）**: 範囲語の射程外・方向語の方向誤りで「届かない」発生時に「ゲームが理不尽」になっていないか（範囲・方向は新たに学ばないといけない要素）
- **H2c（雑魚→ボスの再挑戦意欲）**: マス目移動・FOV・敵 AI 追跡が加わったことで「ボス戦に到達するまでに疲れる」「戦術選択肢が多すぎて麻痺する」になっていないか

### 6.2 再判定の前提条件

- INC-3 まで通過済み（シームレス戦闘の最小実装が安定動作）。
- `06 §6` の S1〜S9 再判定が完了済み（spell_builder 単独での H が新仕様でも真）。
- combat_test を grid-aware バージョンに改修済み（INC-3 / INC-3.5 期間中の試作場、`04 v0.6`）。または `dungeon_view.tscn` 上で 1 部屋 + 雑魚 1 + ボス 1 を配置した検証構成。
- `03 v0.18` 範囲語・方向語のコア違反扱い、scaffold=max 射程プレビュー、`09 §9` UI 拡張がすべて INC-3.5 で実装済み。

### 6.3 再走するシナリオ

§3 の C1〜C5 を **位置あり・シームレス戦闘の状態で再走**する。具体的には:

| ID | 元仕様 | INC-3.5 再走時の修正 | 期待される新観測 |
|----|--------|------------------------|------------------|
| C1 | `meiða fjanda` で雑魚撃破 | 部屋の端に雑魚配置、プレイヤー隣接へ歩いて詠唱（方向語なし、最隣接敵自動） | 2〜3 ターンで撃破できる。**歩く Δ + 詠唱 Δ の累積で「7 日の予算消費」が体感**できる |
| C2 | 誤格 `meiða fjandi` | 同上、誤格 → 修正の流れに「歩いて間合いを取り直す」選択肢を追加 | 戦闘ログで `fjandi → fjanda` 提示。次ターンに修正詠唱で命中する流れが新仕様でも成立 |
| C3 | 雑魚 → ボス遷移 | 雑魚を 1 体目部屋、ボスを 2 体目部屋に配置。階段や扉を経由して移動 | ボス出現演出（v0.4 実装済み）が **シームレス型では「ボスの部屋に入った瞬間」**に発火するか。導線が自然か |
| C4 | scaffold=max で初学者 | `09 §9` のライブ射程プレビュー + 方向矢印 + AoE 範囲のフル補助下 | **5 分以内に雑魚撃破** が新仕様でも達成可能か（§4.1.1 の戦闘版） |
| C5 | scaffold=none で達人 | 補助なし、`fram meiða fjanda` や `vítt eldr meiða fjanda` 等の位置あり詠唱 | 暴発ゼロ・誤射ゼロ・ボス安定撃破。**範囲語・方向語の正しい使用で AoE が決まる気持ちよさ**が体感できるか |

加えて **新規シナリオ C6〜C8** を追加（INC-3.5 で `tests/scenarios/combat/` に固定化）:

| ID | 状況 | scaffold | C | 期待 |
|----|------|----------|---|------|
| C6 | 複数敵への AoE 詠唱 (`vítt eldr meiða fjanda` で 3 体まとめて) | mid | 50 | AoE で 3 体に同時着弾。総ダメージが単発 ×3 で過剰でないこと。`09 §11` の AoE 倍率検討の判定材料 |
| C7 | 貫通詠唱 (`í gegnum fram meiða fjanda` で壁越しの敵を貫通) | mid | 70 | 壁越し攻撃の体感が「ズルしてる気持ちよさ」になるか「強すぎる」か |
| C8 | 距離取り戦術（プレイヤーが歩いて敵から距離を取り、`fjarri meiða fjanda` で遠距離攻撃） | low | 60 | 距離概念が戦術選択を生むか。「敵が追跡してくるので逃げながら撃つ」体験が成立するか |

### 6.4 通過条件（新仕様 H2 真の判定）

§4.1 の元 5 条件に加え、以下を新規追加。**全条件を新仕様でも満たす**ことが H2 真の判定基準。

1. **§4.1.1 発見可能性（戦闘版・C4）の維持**: scaffold=max のシームレス戦闘で 5 分以内に雑魚撃破できる。位置取り・方向指定が「自明な操作」として吸収されている。
2. **§4.1.2 手応えの可視性の維持**: 戦闘 UI の情報量増加（最隣接敵 HP・推定脅威度・対象タイル・方向矢印・射程プレビュー）が `GrammarReport` の視認性を奪っていないこと。
3. **§4.1.3 誤射→修正の維持**: 位置あり状態でも修正詠唱で命中できる。**新規**: 射程外詠唱 (`nær` で遠い敵) の場合、「歩いて近づく」か「`fjarri` に変える」かの 2 択を被験者が自然に選べる。
4. **新規 H2 拡張（AoE 体感）**: C6 で AoE 詠唱が「広範囲を撃ち抜く爽快感」を生む。範囲攻撃の体験が攻撃魔法の魅力を上げる。
5. **新規 H2 拡張（距離戦術）**: C8 で距離取り（移動 + 遠距離詠唱）の戦術選択が成立する。「歩く Δ vs 詠唱 Δ」の二択が戦闘内ジレンマとして体感される。
6. **新規 H2 拡張（自己帰責の維持）**: 「届かない」「方向違い」発生時に「自分の指定ミス」と帰責する。「ゲームが理不尽」にならない。

### 6.5 kill 基準（新仕様 H2 が偽の判定）

§4.3 の元 kill 基準に加え、以下のいずれか該当で H2 は新仕様で偽:

- **§6.4.1 (5 分以内) が破綻**: scaffold=max でも 10 分以上かかる被験者が多数。「画面情報多すぎ」「方向と射程と格を同時に考えられない」の反応。
- **§6.4.2 (UI 視認性) が破綻**: 「GrammarReport がどこにあるか分からない」「ダメージログより射程プレビューを見てしまう」の反応。
- **§6.4.6 (自己帰責) が破綻**: 「届かないって表示されてもどう直せばいいか分からない」「方向の概念が古ノルド語の格と無関係に感じる」の反応。
- **AoE/貫通が壊れる**: C6 で AoE 倍率なしで「総ダメージ ×3 で雑魚を一掃」が「強すぎ→範囲詠唱しか使わなくなる」現象。あるいは C7 の貫通が「ボス含めワンショット」で戦術崩壊。

→ `02 §4` の方向転換: (a) AoE 倍率（`05 BalanceConfig.aoe.damage_split`）を導入して総ダメージを抑制、(b) 範囲語・方向語の可用性を初級+/中級に後ろ倒し、(c) `dungeon_view` のシームレス UI を再設計（戦闘モード時に詠唱 UI を全面化する半シーン切替）、(d) どうしても破綻するなら**マス目移動・グリッド戦術自体を撤回**（最後の手段、`01 v0.3` への回帰）。

### 6.6 結果の記録

再判定の結果は本書の §6.7（次バージョン v0.6 で追加予定）に記録する。書き込み項目:

- 各被験者の C1〜C8 通過/未通過、5 分以内発見の達成有無
- 5 段階自己申告の中央値（§4.2 と同形式）
- 「シームレス戦闘の感想」「AoE 詠唱の感想」「距離戦術の感想」自由記述
- kill 基準に該当した場合、上記 (a)/(b)/(c)/(d) のどの方向転換を選んだか

---

参照（v0.5 追加）: マップ・移動仕様 → `09` / `06 §6`（INC-3.5 における H 再判定の対応章）
